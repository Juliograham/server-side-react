import React, { Component } from 'react';

const Home = () => {
    return (
        <div>
            <button onClick={() => console.log('Hi there!')}>Press Me!</button>
        </div>
    )
}

export default Home;